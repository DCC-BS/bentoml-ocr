apiVersion: v1
kind: Secret
metadata:
  name: glm-ocr-proxy-secrets
  labels:
    app.kubernetes.io/name: glm-ocr-proxy
    app.kubernetes.io/part-of: bentoml-ocr
type: Opaque
stringData:
  VLLM_API_URL: "CHANGE_ME"
  VLLM_MODEL_NAME: "CHANGE_ME"
  # VLLM_API_KEY: "CHANGE_ME"  # provision via SealedSecrets or External Secrets Operator
